{{- if .Values.nginx.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "larakube.fullname" . }}-nginx-config
  labels:
    {{- include "larakube.labels" . | nindent 4 }}
data:
  nginx.conf: |-
{{ tpl .Values.nginx.config.custom . | indent 4 }}
{{- end }} 